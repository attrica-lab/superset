{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "command -v attrica >/dev/null 2>&1 && exec attrica why --hook; exit 0"
          }
        ]
      }
    ]
  }
}
